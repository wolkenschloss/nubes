plugins {
    id "wolkenschloss.testbed"
}

defaultTasks 'start'

testbed {
    domain {
        name = "testbed"
    }
    pool {
        name = "wolkenschloss"
        path = "$buildDir/wolkenschloss"
    }

    poolDirectory = file "$buildDir/hallelulia"
}

//project.buildDir = project.dir "xcd"